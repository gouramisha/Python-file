def merge_array (left , right):
    result = [ ]
    i , j = 0 , 0
    n, num = len(left), len (right)
    while i <n and j <m:
        if left i <= right[j]
           result.append(left[i])
        i+=1 
    else:
     result.append (right[j])
     j+=1.

